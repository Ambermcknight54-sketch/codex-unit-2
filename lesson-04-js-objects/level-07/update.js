// TODO: Update existing properties and export the updated object.
const obj = { age: 0 };
// TODO: update obj.age
export default obj;
// update.js
const obj = { age: 0 };

// Update the existing property
obj.age = 1;

// Export the final updated object
export default obj;