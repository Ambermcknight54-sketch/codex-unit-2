// TODO: Use document.getElementById to select the element and log properties.
// Tasks guide: create a variable `element` and log id, classList, innerText, innerHTML, attributes.
// Example starter:
// const element = document.getElementById('sample');
// console.log(element.id);
//1. Capture the specific DOM element by its ID and store it in a variable
const element = document.getElementById("sample");

// 2. Log the requested properties to the browser developer console
console.log(element.id);
console.log(element.classList);
console.log(element.innerText);
console.log(element.innerHTML);
console.log(element.attributes);

// 3. Pause execution using the debugger to inspect the element object live
debugger;
