// TODO: Select element with getElementById('desc') into `element`, log previous innerText, then set a new string.
// Example starter:
// const element = document.getElementById('desc');
// console.log('before:', element.innerText);
// 1. Target the element using getElementById('desc') into a variable
const element = document.getElementById("desc");

// 2. Log previous innerText
console.log("before:", element.innerText);

// 3. Set element.innerText to a new descriptive string that mentions 'innerText'
element.innerText =
  "The innerText property updates only the visible plain text content of an element.

// 4. Use the debugger to inspect changes
debugger;
