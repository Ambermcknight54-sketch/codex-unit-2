// Level 05 — Declare string and number arrays (same-line, plural names)
// TODO: Declare 5 plural-named string arrays and 5 plural-named number arrays, each with >=3 items.
let userNames = ["Alice", "Bob", "Charlie"];
let cityNames = ["New Orleans", "Austin", "Seattle"];
let productCategories = ["Electronics", "Books", "Clothing"];
let statusMessages = ["Pending", "Active", "Archived"];
let languageCodes = ["english", "spanish", "french"];

let testScores =;
let itemPrices =;
let dailyTemperatures =;
let pageViews =;
let inventoryCounts =;
// Example:
// let userNames = ["Ava", "Jon", "Maya"];

let userNames = [];
// Export one of the arrays as the default
export default userNames;