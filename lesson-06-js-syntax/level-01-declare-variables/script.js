// Level 01 — Declare 10 variables (use `let`)
// TODO: Declare ten `let` variables with NO initial value, one per line.
// Level 01 — Declare 10 variables (use `let`)
export default userFirstName;
// Export one variable as the default
let userFirstName;
let userLastName;
let userEmailAddress;
let userPhoneNumber;
let userAccountStatus;
let userDateOfBirth;
let userShippingAddress;
let userMembershipLevel;
let userTotalPoints;
let userPreferredLanguage;

// Export one variable as the default
export default userFirstName;
// Example:
// let userName;

// Add your lines below:

let userName;
