// Level 08 — Clear the terminal
// TODO: Run `clear` or press Ctrl+L and write a short array or string mentioning `clear` or `ctrl+l`.
export default "I had open a new terminal because clear and command did not work";
