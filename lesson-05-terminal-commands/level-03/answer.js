// Level 03 — Change directory with cd
// TODO: Use `cd` to move into a folder and back; describe your commands here.
export default "I used 'ls' to locate a subfolder, used 'cd' to move into that directory, and then executed 'cd ..' to return to the parent workspace.";
