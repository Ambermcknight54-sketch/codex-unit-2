// Level 05 — Parent directory (..)
// TODO: Use `cd ..` and/or `../` to reference sibling files; describe the steps here.
export default "I used '..' to navigate up to the parent directory and utilized '../' to access sibling folders.";
