// Level 05 — Parent directory (..)
// TODO: Use `cd ..` and/or `../` to reference sibling files; describe the steps here.
export default "I used 'cd ..' to move to the parent directory and ../ to reference a sibling. The ../ simply tells the terminal: ../ 'Go up one level from where I am right now, then look for this folder or file.'";
