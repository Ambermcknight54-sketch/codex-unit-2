// Level 04 — Current directory (.)
// TODO: Run a script using `./` and write a short note here mentioning `.` or `./`.
export default "Executing 'cd .' keeps you in the current directory, and using './' tells the terminal to look for a file in this exact folder.";
